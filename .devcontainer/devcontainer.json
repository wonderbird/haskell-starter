{
	"name": "haskell",
	"dockerFile": "Dockerfile",
	"postCreateCommand": "cabal install --package-env=. --lib hspec hspec-contrib QuickCheck HUnit",

	"settings": { 
		"terminal.integrated.shell.linux": "/bin/bash"
	},

	"extensions": [
		"haskell.haskell"
	]
}
